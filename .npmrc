# Intentionally minimal. pnpm's default isolated node-linker is correct for
# Wrangler 4 + @cloudflare/vite-plugin + Astro 6 (verified — doc 09); no
# hoisting needed. The build-script allowlist lives in pnpm-workspace.yaml
# (onlyBuiltDependencies). Workspace packages link via `workspace:*` deps.
