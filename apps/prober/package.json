{
  "name": "@worldspring/prober",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "node ../../scripts/assert-d1-configured.mjs wrangler.jsonc && wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@worldspring/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
